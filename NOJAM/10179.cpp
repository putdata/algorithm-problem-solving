#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  cout.precision(2);
  cout << fixed;
  while (t--) {
    double x;
    cin >> x;
    cout << "$" << 0.8 * x << '\n';
  }
  return 0;
}